# CMD-help 命令参考文档编写规范

本文档规定了 [`references/CMD-help/`](./references/CMD-help/) 下命令参考文档的格式规范。[`scripts/search_cmd.py`](./scripts/search_cmd.py) 依赖这些结构做索引与查询（通过 CMD-INDEX 索引块定位、从正文【视图】小节补全视图、按块边界切分文档），**新增或修改文档时请严格遵守，否则命令将无法被查询到**。

## 1. 目录与文件命名

```
references/CMD-help/
├── <模块名>/                  # 顶层目录名即模块名（如 VLAN、OSPF、IPsec、环路检测）
│   ├── <模块名>配置命令.md     # 配置命令
│   ├── <模块名>调试命令.md     # 调试命令
│   └── <模块名>Probe命令.md    # Probe 命令
└── ...
```

- 顶层目录名 = 模块名，与 `--file <模块>` 过滤及模块名建议（`suggested_modules`）对应，请使用稳定、易识别的名称。
- 每条命令按功能归入「配置命令 / 调试命令 / Probe命令」三类文件之一。

## 2. 文件头部 CMD-INDEX 索引块（必写）

每个 `.md` 文件顶部必须有 `<!-- CMD-INDEX ... -->` 注释块，供解析器快速定位命令，**禁止全文扫描**：

```md
<!-- CMD-INDEX
  vlan                                | VLAN视图       | L12
  vlan { batch \| when }              | 系统视图         | L120
  rule (IPv4 advanced ACL view)       | IPv4高级ACL视图 | L300
  display vlan                        | 任意视图         | L450
-->
```

规则：
- 以 `<!-- CMD-INDEX` 行开始，以 `-->` 行结束；`-->` 之后必须有空行再进正文。
- 块内每行一条索引，格式：`命令名 | 视图 | L行号`，三列缺一不可。
- **分隔符是未转义的 `|`**；命令名内的花括号备选竖线必须用 `\|` 转义（见第 3 节），解析器按 `(?<!\\)\|` 切分。
- `L行号` 为 1 基行号，**必须指向正文中该命令块标题（`**模块 \-- 分组 \-- 命令名**`）所在行**，否则块内容切分错误。
- 行号要求自上而下递增（索引行顺序与正文顺序一致，按命令名排序即可）。
- 无 CMD-INDEX 块或格式不符的文件会被静默跳过——该文件的所有命令都无法查询。

## 3. 命令名书写

命令名列中，命令的真实名称只允许出现两种注记：

**括号注记**——区分同名但视图/语义不同的命令族，`--exact` 会剥掉括号后比对：
```
rule (IPv4 advanced ACL view)
rule (IPv6 advanced ACL view)
priority (IKE keychain view)
priority (IKE profile view)
match local address (IKE keychain view)
```

**花括号备选**——把同一命令的不同写法合并成一行，`--exact` 会展开成每个真实写法：
```
ipsec { ipv6-policy \| policy }
display ipsec { ipv6-policy-template \| policy-template }
```
花括号内的竖线必须写 `\|`（反斜杠转义），否则会被当作列分隔符导致索引行错乱。

**视图列**：
- 多个视图用 `/` 连接（如 `IPsec安全策略视图/IPsec安全策略模板视图`）。
- 视图列允许留空，但此时**正文【视图】小节必须准确填写**（见第 4 节），解析器会从正文提取；正文也没有时才会继承同文件最近一条非空视图。
- 视图名称请与官方术语一致，`--view <视图>` 过滤按子串匹配视图名。

## 4. 正文命令块结构

索引的每一行 `L行号` 指向一个命令块。块结构：

```md
**VLAN \-- VLAN配置命令 \-- vlan**        ← 块标题（索引 L 行号指向此行）

------------------------------------------------------------------------  ← 整行分隔线（10+ 连字符）

[vlan]命令用来创建VLAN并进入VLAN视图。...   ← 命令说明（可多行）

【命令】

vlan vlan-id

【视图】

VLAN视图

【参数】

vlan-id：VLAN编号，取值范围为1～4094。

【描述】

...

【举例】

# 创建VLAN 2。
<Sysname> system-view
[Sysname] vlan 2
```

规则：
- **块标题**格式：`**模块 \-- 分组 \-- 命令名**`，其中 `\--` 是反斜杠转义的分隔符；必须与索引 `L行号` 一一对应。
- 标题后紧跟**整行分隔线**（`----`，至少 10 个连字符），解析器用它兜底切分块边界。
- 小节标题用全角方括号：`【命令】`、`【视图】`、`【缺省情况】`、`【参数】`、`【描述】`、`【使用指导】`、`【举例】`、`【相关命令】`。小节标题允许带尾随 `]` 注记（解析按前缀匹配，兼容）。
- `【视图】` 小节：值紧跟在小节标题后的非空行；允许多行（多个视图分行写），解析器用 `/` 连接。**当索引视图列为空时，这里就是唯一的数据来源，必须填写**。
- `【命令】` 小节：给出真实命令语法，`--full` 的紧凑建议取其前 5 行。
- 命令块之间用分隔线严格隔开，正文内不要出现游离的 `----` 分隔线。

## 5. 更新文档的注意事项

- **改文档文件**（新增/修改/删除命令、编辑标题行号）：[`search_cmd.py`](./scripts/search_cmd.py) 经 `git status` 检测到 [`references/CMD-help`](./references/CMD-help/) 变化后会自动重建索引缓存，无需手工处理；非 Git 环境按「路径+修改时间」哈希比对，同样自动重建。
- **改解析/匹配逻辑**（只改 [`scripts/search_cmd.py`](./scripts/search_cmd.py)，不改文档）：md 文件时间戳未变，缓存不会自动重建，**必须手动删除 [`references/.cmd_cache.jsonl`](./references/.cmd_cache.jsonl)** 强制重建。
- **新增模块**：新建目录 + 三类文件，并在每个文件写好 CMD-INDEX 索引块。
- **常见错误排查**：
  - 命令查不到 → 检查该文件是否漏写 CMD-INDEX、`L行号` 是否指向块标题行、视图列分隔符是否被花括号内的 `\|` 错拆。
  - `--view` 过滤命中不了 → 检查索引视图列为空时正文【视图】是否填写正确。
  - `--file` 过滤为空但模块存在 → 检查顶层目录名拼写，可用返回的 `suggested_modules` 提示校准模块名。

## 6. 高频命令白名单

[`references/high-frequency-commands.md`](./references/high-frequency-commands.md) 收录已确认的通用 H3C 语法命令，查询时优先命中白名单，无需文档。确属通用语法的命令可加入白名单，但请确保不与其他模块的文档冲突。
